import Layout from "@/components/Layout";

const TeacherReview = () => {
  return (
    <Layout role="teacher" title="Review" subtitle="Review student work">
      <div className="text-center text-gray-400 py-10">No review data to display.</div>
    </Layout>
  );
};

export default TeacherReview;
